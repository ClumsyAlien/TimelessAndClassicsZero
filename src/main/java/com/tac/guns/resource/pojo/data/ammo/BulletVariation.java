package com.tac.guns.resource.pojo.data.ammo;

import com.google.gson.annotations.SerializedName;

public class BulletVariation {
    // Switch from Enum static to creating an object per bullet variation
    // Standard bullets won't need a suffix, or definition
    public BulletVariation() {}
}
